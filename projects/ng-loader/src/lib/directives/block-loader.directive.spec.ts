import { BlockLoaderDirective } from './block-loader.directive';

describe('BlockLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
